export const configurations = {
	canvas: document.getElementById('gameArea'),
	ctx: this.canvas.getContext('2d'),
	scale: 10,
	rows: this.canvas.height / this.scale,
	columns: canvas.width / this.scale
};

